import { defineStore } from 'pinia'

/**
 * 标签页面服务器配置 store
 * 提供标签页面的服务器相关配置
 */
export const useServerPageTagStore = defineStore('serverPageTag', () => {
  // 这个store在原始实现中可能是空的或包含简单配置
  // 为保持一致性，创建基本的Pinia store结构
  return {
    // 可以根据需要添加状态和方法
  }
})