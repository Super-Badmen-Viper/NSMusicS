import { defineStore } from 'pinia'

/**
 * 推荐页面服务器配置 store
 * 提供推荐页面的服务器相关配置
 */
export const useServerPageRecommendStore = defineStore('serverPageRecommend', () => {
  // 这个store在原始实现中可能是空的或包含简单配置
  // 为保持一致性，创建基本的Pinia store结构
  return {
    // 可以根据需要添加状态和方法
  }
})