import { defineStore } from 'pinia'

/**
 * 专辑页面服务器配置 store
 * 提供专辑页面的服务器相关配置
 */
export const useServerPageAlbumStore = defineStore('serverPageAlbum', () => {
  // 这个store在原始实现中是空的，所以这里创建一个空的Pinia store
  // 可以根据需要添加状态和方法
  return {
    // 空store
  }
})