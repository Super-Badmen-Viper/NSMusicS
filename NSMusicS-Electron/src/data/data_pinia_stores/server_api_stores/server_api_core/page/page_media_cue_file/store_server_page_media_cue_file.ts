import { defineStore } from 'pinia'

/**
 * CUE媒体文件页面服务器配置 store
 * 提供CUE媒体文件页面的服务器相关配置
 */
export const useServerPageMediaCueFileStore = defineStore('serverPageMediaCueFile', () => {
  // 这个store在原始实现中可能是空的或包含简单配置
  // 为保持一致性，创建基本的Pinia store结构
  return {
    // 可以根据需要添加状态和方法
  }
})