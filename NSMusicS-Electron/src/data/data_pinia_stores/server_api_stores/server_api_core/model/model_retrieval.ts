import { defineStore } from 'pinia'

/**
 * 服务器检索模型状态管理
 * 注：当前为空实现，后续可根据需要添加状态和方法
 */
export const useServerModelRetrievalStore = defineStore('serverModelRetrieval', () => {
  // 后续可添加状态

  return {
    // 后续可添加方法
  }
})