# Environment setup (Windows) — PowerShell notes
Write-Host "Env setup (Windows) — manual steps"

Write-Host "1) Install nvm-windows: https://github.com/coreybutler/nvm-windows"
Write-Host "2) Open new PowerShell and install/use Node: nvm install 18 ; nvm use 18"
Write-Host "3) In project root, run: npm install"
Write-Host "4) If native modules need rebuild (Electron), consider using electron-rebuild or rebuild scripts"

Write-Host "This file contains guidance only — run steps manually in elevated PowerShell if needed."